"use client";
import { useState } from "react"; import { Pencil, Plus, X } from "lucide-react"; import { Button } from "@/components/ui/button"; import { DestinationForm } from "@/components/admin/destination-form"; import type { DestinationDTO } from "@/types";
export function DestinationDrawer({ destination }: { destination?: DestinationDTO }) { const [open, setOpen] = useState(false); return <>{destination ? <Button variant="outline" size="sm" onClick={() => setOpen(true)}><Pencil className="size-3.5" />Edit</Button> : <Button variant="gradient" onClick={() => setOpen(true)}><Plus className="size-4" />Add Destination</Button>}{open ? <div className="fixed inset-0 z-[80]"><button className="absolute inset-0 bg-black/45" onClick={() => setOpen(false)} aria-label="Close destination form" /><aside className="absolute right-0 top-0 flex h-full w-full max-w-2xl flex-col border-l bg-background shadow-2xl"><div className="flex items-center justify-between border-b p-4"><div><h2 className="text-lg font-bold">{destination ? "Edit Destination" : "Add Destination"}</h2><p className="text-sm text-muted-foreground">{destination?.title ?? "Create destination content"}</p></div><Button variant="ghost" size="icon" onClick={() => setOpen(false)}><X /></Button></div><div className="min-h-0 flex-1 overflow-y-auto p-5"><DestinationForm destination={destination} onSaved={() => setOpen(false)} onCancel={() => setOpen(false)} /></div></aside></div> : null}</>; }
