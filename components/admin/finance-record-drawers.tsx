"use client";

import { useState } from "react"; import { useRouter } from "next/navigation"; import { Loader2, Pencil, Plus, X } from "lucide-react"; import { toast } from "sonner"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Select } from "@/components/ui/select"; import { Textarea } from "@/components/ui/textarea"; import { EXPENSE_STATUSES, INVOICE_STATUSES } from "@/lib/constants"; import type { ExpenseDTO, InvoiceDTO } from "@/types";
const Mark = () => <span className="text-destructive">*</span>; const today = () => new Date().toISOString().slice(0, 10);
function DrawerFrame({ title, subtitle, open, close, children }: { title: string; subtitle: string; open: boolean; close: () => void; children: React.ReactNode }) { return open ? <div className="fixed inset-0 z-[80]"><button className="absolute inset-0 bg-black/45" onClick={close} aria-label="Close form" /><aside className="absolute right-0 top-0 flex h-full w-full max-w-xl flex-col border-l bg-background shadow-2xl"><div className="flex items-center justify-between border-b p-4"><div><h2 className="text-lg font-bold">{title}</h2><p className="text-sm text-muted-foreground">{subtitle}</p></div><Button variant="ghost" size="icon" onClick={close}><X /></Button></div><div className="min-h-0 flex-1 overflow-y-auto p-5">{children}</div></aside></div> : null; }
export function ExpenseDrawer({ expense }: { expense?: ExpenseDTO }) { const router = useRouter(); const [open, setOpen] = useState(false); const [loading, setLoading] = useState(false); const [form, setForm] = useState({ title: expense?.title ?? "", category: expense?.category ?? "Operations", vendor: expense?.vendor ?? "", amount: expense?.amount ?? 0, expenseDate: expense?.expenseDate?.slice(0, 10) ?? today(), status: expense?.status ?? "pending", paymentReference: expense?.paymentReference ?? "", notes: expense?.notes ?? "" }); const set = (key: keyof typeof form, value: string | number) => setForm((current) => ({ ...current, [key]: value })); async function submit(e: React.FormEvent) { e.preventDefault(); setLoading(true); try { const response = await fetch(expense ? `/api/admin/expenses/${expense._id}` : "/api/admin/expenses", { method: expense ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, amount: Number(form.amount) }) }); const data = await response.json(); if (!response.ok || !data.success) throw new Error(data.message || "Unable to save expense"); toast.success("Expense saved"); setOpen(false); router.refresh(); } catch (error) { toast.error((error as Error).message); setLoading(false); } } return <>{expense ? <Button variant="ghost" size="icon" onClick={() => setOpen(true)}><Pencil className="size-4" /></Button> : <Button variant="gradient" onClick={() => setOpen(true)}><Plus className="size-4" />Add Expense</Button>}<DrawerFrame open={open} close={() => setOpen(false)} title={expense ? "Edit Expense" : "Add Expense"} subtitle={expense?.expenseNumber ?? "Record an operational expense"}><form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><Label className="mb-1 block">Title <Mark /></Label><Input required value={form.title} onChange={(e) => set("title", e.target.value)} /></div><div><Label className="mb-1 block">Category <Mark /></Label><Input required value={form.category} onChange={(e) => set("category", e.target.value)} /></div><div><Label className="mb-1 block">Vendor</Label><Input value={form.vendor} onChange={(e) => set("vendor", e.target.value)} /></div><div><Label className="mb-1 block">Amount <Mark /></Label><Input required type="number" min="0.01" step="0.01" value={form.amount} onChange={(e) => set("amount", Number(e.target.value))} /></div><div><Label className="mb-1 block">Expense date <Mark /></Label><Input required type="date" value={form.expenseDate} onChange={(e) => set("expenseDate", e.target.value)} /></div><div><Label className="mb-1 block">Status <Mark /></Label><Select value={form.status} onChange={(e) => set("status", e.target.value)}>{EXPENSE_STATUSES.map((item) => <option key={item}>{item}</option>)}</Select></div><div><Label className="mb-1 block">Payment reference</Label><Input value={form.paymentReference} onChange={(e) => set("paymentReference", e.target.value)} /></div><div className="sm:col-span-2"><Label className="mb-1 block">Notes</Label><Textarea value={form.notes} onChange={(e) => set("notes", e.target.value)} /></div><div className="flex justify-end gap-2 border-t pt-4 sm:col-span-2"><Button type="button" variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button type="submit" variant="gradient" disabled={loading}>{loading ? <Loader2 className="size-4 animate-spin" /> : null}Save</Button></div></form></DrawerFrame></>; }
export function InvoiceDrawer({ invoice }: { invoice?: InvoiceDTO }) { const router = useRouter(); const [open, setOpen] = useState(false); const [loading, setLoading] = useState(false); const [form, setForm] = useState({ customerName: invoice?.customerName ?? "", customerEmail: invoice?.customerEmail ?? "", customerPhone: invoice?.customerPhone ?? "", description: invoice?.description ?? "", amount: invoice?.amount ?? 0, issueDate: invoice?.issueDate?.slice(0, 10) ?? today(), dueDate: invoice?.dueDate?.slice(0, 10) ?? today(), status: invoice?.status ?? "draft", bookingNumber: invoice?.bookingNumber ?? "", notes: invoice?.notes ?? "" }); const set = (key: keyof typeof form, value: string | number) => setForm((current) => ({ ...current, [key]: value })); async function submit(e: React.FormEvent) { e.preventDefault(); setLoading(true); try { const response = await fetch(invoice ? `/api/admin/invoices/${invoice._id}` : "/api/admin/invoices", { method: invoice ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, amount: Number(form.amount) }) }); const data = await response.json(); if (!response.ok || !data.success) throw new Error(data.message || "Unable to save invoice"); toast.success("Invoice saved"); setOpen(false); router.refresh(); } catch (error) { toast.error((error as Error).message); setLoading(false); } } return <>{invoice ? <Button variant="ghost" size="icon" onClick={() => setOpen(true)}><Pencil className="size-4" /></Button> : <Button variant="gradient" onClick={() => setOpen(true)}><Plus className="size-4" />New Invoice</Button>}<DrawerFrame open={open} close={() => setOpen(false)} title={invoice ? "Edit Invoice" : "New Invoice"} subtitle={invoice?.invoiceNumber ?? "Create a customer invoice"}><form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><Label className="mb-1 block">Customer name <Mark /></Label><Input required value={form.customerName} onChange={(e) => set("customerName", e.target.value)} /></div><div><Label className="mb-1 block">Email</Label><Input type="email" value={form.customerEmail} onChange={(e) => set("customerEmail", e.target.value)} /></div><div><Label className="mb-1 block">Phone</Label><Input value={form.customerPhone} onChange={(e) => set("customerPhone", e.target.value)} /></div><div className="sm:col-span-2"><Label className="mb-1 block">Description <Mark /></Label><Input required value={form.description} onChange={(e) => set("description", e.target.value)} /></div><div><Label className="mb-1 block">Amount <Mark /></Label><Input required type="number" min="0.01" step="0.01" value={form.amount} onChange={(e) => set("amount", Number(e.target.value))} /></div><div><Label className="mb-1 block">Booking number</Label><Input value={form.bookingNumber} onChange={(e) => set("bookingNumber", e.target.value)} /></div><div><Label className="mb-1 block">Issue date <Mark /></Label><Input required type="date" value={form.issueDate} onChange={(e) => set("issueDate", e.target.value)} /></div><div><Label className="mb-1 block">Due date <Mark /></Label><Input required type="date" min={form.issueDate} value={form.dueDate} onChange={(e) => set("dueDate", e.target.value)} /></div><div className="sm:col-span-2"><Label className="mb-1 block">Status <Mark /></Label><Select value={form.status} onChange={(e) => set("status", e.target.value)}>{INVOICE_STATUSES.map((item) => <option key={item}>{item}</option>)}</Select></div><div className="sm:col-span-2"><Label className="mb-1 block">Notes</Label><Textarea value={form.notes} onChange={(e) => set("notes", e.target.value)} /></div><div className="flex justify-end gap-2 border-t pt-4 sm:col-span-2"><Button type="button" variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button type="submit" variant="gradient" disabled={loading}>{loading ? <Loader2 className="size-4 animate-spin" /> : null}Save</Button></div></form></DrawerFrame></>; }
