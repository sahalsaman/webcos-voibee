"use client";
import { useState } from "react"; import { Pencil, Plus, X } from "lucide-react"; import { Button } from "@/components/ui/button"; import { EventForm } from "@/components/admin/event-form"; import type { EventDTO } from "@/types";
export function EventDrawer({ event }: { event?: EventDTO }) { const [open, setOpen] = useState(false); return <>{event ? <Button variant="outline" size="sm" onClick={() => setOpen(true)}><Pencil className="size-3.5" />Edit</Button> : <Button variant="gradient" onClick={() => setOpen(true)}><Plus className="size-4" />Add Event</Button>}{open ? <div className="fixed inset-0 z-[80]"><button className="absolute inset-0 bg-black/45" onClick={() => setOpen(false)} aria-label="Close event form" /><aside className="absolute right-0 top-0 flex h-full w-full max-w-2xl flex-col border-l bg-background shadow-2xl"><div className="flex items-center justify-between border-b p-4"><div><h2 className="text-lg font-bold">{event ? "Edit Event" : "Add Event"}</h2><p className="text-sm text-muted-foreground">{event?.title ?? "Create a major event"}</p></div><Button variant="ghost" size="icon" onClick={() => setOpen(false)}><X /></Button></div><div className="min-h-0 flex-1 overflow-y-auto p-5"><EventForm event={event} onSaved={() => setOpen(false)} onCancel={() => setOpen(false)} /></div></aside></div> : null}</>; }
