import { Schema, model, models } from "mongoose"; import { LEAVE_REQUEST_STATUSES, LEAVE_TYPES } from "@/lib/constants";
const schema=new Schema({employee:{type:Schema.Types.ObjectId,ref:"Employee",required:true,index:true},type:{type:String,enum:LEAVE_TYPES,required:true},startDate:{type:Date,required:true,index:true},endDate:{type:Date,required:true},days:{type:Number,required:true,min:.5},status:{type:String,enum:LEAVE_REQUEST_STATUSES,default:"pending",index:true},reason:{type:String,required:true,trim:true},adminNotes:{type:String,default:"",trim:true}},{timestamps:true}); export default models.LeaveRequest||model("LeaveRequest",schema);
