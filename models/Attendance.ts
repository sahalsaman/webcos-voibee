import { Schema, model, models } from "mongoose"; import { ATTENDANCE_STATUSES } from "@/lib/constants";
const schema=new Schema({employee:{type:Schema.Types.ObjectId,ref:"Employee",required:true,index:true},date:{type:Date,required:true,index:true},status:{type:String,enum:ATTENDANCE_STATUSES,required:true,index:true},checkIn:{type:String,default:""},checkOut:{type:String,default:""},workHours:{type:Number,default:0,min:0,max:24},notes:{type:String,default:"",trim:true}},{timestamps:true}); schema.index({employee:1,date:1},{unique:true}); export default models.Attendance||model("Attendance",schema);
